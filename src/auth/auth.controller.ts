/**
 * Intentionally no Shop Assistant login/register controller.
 *
 * Credential collection, account creation, token refresh, and role assignment
 * are owned by the authentication service. Shop Assistant only validates bearer
 * tokens through AuthService for protected customer and admin APIs.
 */
