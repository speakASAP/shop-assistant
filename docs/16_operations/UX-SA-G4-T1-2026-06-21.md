# UX-SA-G4-T1-2026-06-21: Current Production UX Improvement Report

Date: 2026-06-21
Task: SA-G4-T1 UX improvement report from session data
Repository root: /home/ssf/Documents/Github/shop-assistant
Evidence files:

- reports/ux/sa-g4-t1-production-aggregate-metrics-2026-06-21.json
- reports/search/sa-g1-failed-search-top20-2026-06-21.json

## Intent Preservation Chain

- Vision: Shop Assistant helps customers turn product needs into comparable offers while preserving privacy, legal transparency, and truthful result handling.
- Goal Impact: SA-G4 gives admins and operators enough observability to improve assistant quality without exposing unnecessary raw production data.
- System: `shop-assistant` production application at `https://shop-assistant.alfares.cz`, backed by the running Kubernetes deployment in namespace `statex-apps`.
- Feature: Privacy-safe UX analytics for public request flow, test interface, dashboard history, saved criteria, selected products, feedback, and protected admin diagnostics.
- Task: SA-G4-T1, "Generate UX improvement report based on session data."
- Execution Plan: Use aggregate-only metrics from the running pod plus static route/source inspection; do not use raw queries, raw messages, profile names tied to users, lead contacts, JWTs, secrets, or raw DB rows.
- Coding Prompt: Report-only lane. No implementation, deployment, shared state edit, or shared validation-report edit in this lane.
- Code: Report artifact only: `docs/16_operations/UX-SA-G4-T1-2026-06-21.md`.
- Validation: Aggregate metrics command, static source inspection, live route checks, `git diff --check`, and focused sensitive-data scan.

## Privacy Method

This report uses aggregate production metrics and static workflow inspection only. It does not include raw query text, message content, profile names, lead contacts, JWTs, secrets, merchant URLs, or database URLs.

## Data Window

- First session: 2026-06-13T03:48:23.996Z
- Last session: 2026-06-21T08:51:59.876Z
- Sessions: 21
- Query sessions: 15
- Search runs: 15
- Search results: 89
- Zero-result search runs: 6
- Messages: 44
- Agent communications: 157
- Agent errors: 0

## Funnel Metrics

| Metric | Value |
| --- | ---: |
| Query session rate | 71.4% |
| Zero-result run rate | 40% |
| Session choice rate | 0% |
| Result-session choice rate | 0% |
| Feedback after results rate | 0% |
| Profile adoption rate | 9.5% |
| Agent error session rate | 0% |

## Distribution Metrics

| Metric | Average | P50 | P90 | Max |
| --- | ---: | ---: | ---: | ---: |
| Search runs per session | 0.71 | 1 | 1 | 1 |
| Results per search run | 5.93 | 10 | 10 | 10 |

Result buckets: {"0":6,"5+":9}

## Findings

1. Zero-result handling remains a primary UX focus. The production aggregate shows 6 zero-result runs out of 15 search runs (40%). The failed-search top-20 artifact currently contains 6 hashed failed-search fingerprints, each occurring once, so there is no repeated production pattern safe enough to tune against without raw data.

2. Product selection is not being used. The aggregate shows 0 selected products and a 0% session choice rate despite 89 persisted results. The next UX improvement should make selection and merchant redirect actions more prominent on result cards and visible in the customer dashboard.

3. Feedback/refinement is not being used. The aggregate shows 0 feedback messages and a 0% feedback-after-results rate. The public result and dashboard session-detail flows should make refinement the natural next step after both successful and zero-result searches.

4. Account features exist but adoption is low. There are 2 profiles and 2 saved criteria records, but only 2 profile-scoped sessions and saved-criteria session usage is not currently reported by the aggregate script. The dashboard should highlight profile and saved-search reuse as part of request creation.

5. Operational reliability looks healthy in this sample. Agent error count is 0, and the latest strict token-file smoke passed separately, so the UX recommendations are product-flow improvements rather than stability repairs.

## Static Workflow Findings

- `public/index.html` exposes dashboard/admin entry points, legal links, cookie consent, AI transparency, public search, and lead capture. Product copy promises selected products, saved criteria, feedback, dashboards, and admin controls.
- `public/test.html` supports session creation, query submission, progress states, grouped products, intermediary results, refine input, final results, chat refinement, profile selection, saved searches, and an admin-flow link. Profiles and saved searches are hidden unless a customer Auth token exists.
- `public/dashboard.html` uses Auth-hosted login, state validation, session-scoped token storage, account-scoped `/api/me` calls, dashboard history, selected products, saved searches, profiles, filters, pagination, and session detail. 401/403 responses hide the private dashboard and clear stale auth.
- `public/admin.html` uses Auth-hosted admin sign-in and validates role through protected admin endpoints. Admin overview includes sessions, authenticated sessions, search runs, search results, selected products, leads, profiles, saved searches, recent zero-result searches, and agent errors.
- `src/admin/operations.controller.ts` redacts message, search-run, agent-communication, and lead sensitive content by default unless an authorized admin explicitly requests sensitive detail.

## Live Route Checks

| Route | HTTP |
| --- | ---: |
| `/` | 200 |
| `/test.html` | 200 |
| `/dashboard.html` | 200 |
| `/admin.html` | 200 |
| `/privacy` | 200 |
| `/cookies` | 200 |
| `/terms` | 200 |
| `/health` | 200 |

## Recommendations

1. Add a stronger result-card selected-product affordance and dashboard selected-products follow-up path.
2. Add visible refinement prompts after results and zero-result guidance, with one-click transfer into the feedback/refine flow.
3. Add saved-search and profile reuse prompts near the composer for authenticated users.
4. Extend aggregate UX metrics to count sessions started from saved criteria, selected-product click-throughs, and refinement attempts.
5. Keep failed-search analysis hashed/aggregate-only unless the owner approves a redacted production extract.

## Validation

- Production aggregate metrics generated inside the running Kubernetes pod using scripts/aggregate-ux-production-metrics.js.
- Static inspection covered public/index.html, public/dashboard.html, and public/admin.html.
- Static inspection also covered public/test.html and protected admin operations redaction behavior.
- Live route checks returned 200 for public app, advanced test, dashboard, admin, legal aliases, and health.
- `git diff --check` passed before report integration.
- No code deployment was run for this report.

## Residual Limitations

- Aggregate metrics do not distinguish organic customer traffic from validation, smoke, or synthetic operational sessions.
- The current aggregate script does not calculate saved-criteria session usage even though the schema supports it through `Session.usedSavedCriteriaId`.
- This report intentionally does not inspect raw search text, raw messages, raw lead content, profile names tied to users, or raw DB rows.
- Authenticated customer/admin browser behavior was inferred from current static source and prior task evidence; no new token-backed browser run was performed in this report lane.
- Recommendations are product/UX follow-up candidates, not implemented changes.
