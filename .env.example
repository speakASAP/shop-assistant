# ============================================
# Shop Assistant - Environment Configuration. Single Source of Truth
# ============================================
# Service Domain - Used by nginx-microservice for auto-registry (e.g. shop-assistant.statex.cz)
# Service Name - Used for logging and service identification
NODE_ENV=
DOMAIN=
SERVICE_NAME=

# ============================================
# External Shared Microservices
# ============================================
AUTH_SERVICE_URL=
# Leads (CRM/confirmations). Contact form -> POST /api/leads/submit. Docker: http://leads-microservice:4400; prod: https://leads.${DOMAIN}
LEADS_SERVICE_URL=
LOGGING_SERVICE_URL=
LOGGING_SERVICE_API_PATH=
AI_SERVICE_URL=

# ============================================
# Database Server (Shared PostgreSQL)
# ============================================
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=
# Build from above or set explicitly: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL=

# ============================================
# Prisma Engine Configuration
# ============================================
PRISMA_SCHEMA_ENGINE_BINARY=
PRISMA_QUERY_ENGINE_BINARY=

# ============================================
# Redis Cache (Shared Redis) - Optional
# ============================================
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
REDIS_DB=

# ============================================
# Service Ports
# ============================================
# Must be 45xx for blue/green (e.g. 4500 blue, 4501 green). See README.
PORT=
PORT_GREEN=

# ============================================
# Logging Configuration
# ============================================
LOG_LEVEL=
LOG_TIMESTAMP_FORMAT=

# ============================================
# CORS Configuration
# ============================================
CORS_ORIGIN=

# ============================================
# Network Configuration
# ============================================
NGINX_NETWORK_NAME=

# ============================================
# Agent execution mode (Option A queue / Option B sync)
# ============================================
# sync = direct in-process calls (Option B). queue = in-memory queue with limited concurrency (Option A).
AGENT_EXECUTION_MODE=
# Max parallel queued SEARCH jobs when mode=queue (default 3).
AGENT_QUEUE_CONCURRENCY=

# ============================================
# HTTP Timeout Configuration
# ============================================
HTTP_TIMEOUT=
GATEWAY_TIMEOUT=
AUTH_SERVICE_TIMEOUT=
LOGGING_SERVICE_TIMEOUT=
AI_SERVICE_TIMEOUT=

# ============================================
# Retry Configuration
# ============================================
RETRY_MAX_ATTEMPTS=
RETRY_DELAY_MS=
AUTH_RETRY_MAX_ATTEMPTS=
AUTH_RETRY_DELAY_MS=

# Frontend URL (optional; CORS_ORIGIN can be used instead)
FRONTEND_URL=

# ============================================
# Legal / Company - used in Privacy, Cookies, Terms (EU AI Act)
# ============================================
COMPANY_LEGAL_NAME=
COMPANY_ICO=
COMPANY_DIC=
COMPANY_ADDRESS=
COMPANY_PHONE=
LEGAL_EMAIL=
PRIVACY_EMAIL=
DPO_EMAIL=
LEGAL_JURISDICTION=
