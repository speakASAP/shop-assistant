apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-assistant
  namespace: statex-apps
  labels:
    app: shop-assistant
    managed-by: k8s-migration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shop-assistant
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: shop-assistant
    spec:
      containers:
        - name: app
          image: localhost:5000/shop-assistant:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4500
              name: http
          envFrom:
            - configMapRef:
                name: shop-assistant-config
            - secretRef:
                name: shop-assistant-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 4500
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 4500
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
