# API Routes Configuration
# Routes are automatically routed to backend service by nginx-microservice deploy-smart.sh
# Auth routes listed first so config regeneration is triggered when they were missing (ensure_blue_green_configs checks first route)

/api/auth
/api/auth/login
/api/auth/register
/api/leads
/api/sessions
/api/sessions/query
/api/sessions/feedback
/api/sessions/results
/api/sessions/choice
